"use strict";
exports.__esModule = true;
exports.getFunctionalInterface = void 0;
var utils_1 = require("./utils");
function getFunctionalInterface(instance) {
    return function (functionName) {
        var functionArguments = [];
        for (var _i = 1; _i < arguments.length; _i++) {
            functionArguments[_i - 1] = arguments[_i];
        }
        if (functionName && utils_1.isFunction(instance[functionName])) {
            // @ts-ignore
            instance[functionName].apply(instance, functionArguments);
        }
        else {
            console.warn("The method `" + functionName + "` doesn't exist.");
        }
    };
}
exports.getFunctionalInterface = getFunctionalInterface;
